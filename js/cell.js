export class Cell {
  constructor(x, y, width, height, text, colIndex, rowIndex) {
    this.x = x;
    this.y = y;
    this.width = width;
    this.height = height;
    this.text = text;
    this.colIndex = colIndex;
    this.rowIndex = rowIndex;

  }
}